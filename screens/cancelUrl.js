import {
    Text,
    View,
    Image,
    TouchableOpacity,
    ImageBackground,
    ScrollView,
    ActivityIndicator,
    Alert,
    StatusBar,
    SafeAreaView,
    Platform
} from "react-native";
export default function CancelUrl({ navigation }) {
    return (
        <View>
            Order cancel
        </View>
    )
}