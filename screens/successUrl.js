import {
    Text,
    View,
    Image,
    TouchableOpacity,
    ImageBackground,
    ScrollView,
    ActivityIndicator,
    Alert,
    StatusBar,
    SafeAreaView,
    Platform
} from "react-native";
export default function SuccessUrl({ navigation }) {
    return (
        <View>
            Thank You for your order
        </View>
    )
}